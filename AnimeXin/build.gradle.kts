version = 4

cloudstream {
    language = "id"
    description = "AnimeXin - Streaming Download Donghua Subtitle Indonesia English"
    authors = listOf("Agoose")

    // Beta-only until the module is compiled and validated inside the target Cloudstream repository.
    status = 3

    tvTypes = listOf(
        "Movie",
        "Anime",
        "OVA",
    )
}
